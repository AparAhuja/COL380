#include <iostream>
#include "task.h"

using namespace std;

Task::Task(Graph& g, int startk, int endk, bool verbose)
: graph(g), startk(startk), endk(endk), verbose(verbose) {}

void Task::run() {
    // TODO: implement function to perform task 1 and output results
}

